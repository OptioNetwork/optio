name: Build Linux Binary

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to build (e.g., 1.0.0)'
        required: true
        default: '1.0.0'
      create_release:
        description: 'Create GitHub release'
        required: false
        default: 'true'
      prerelease:
        description: 'Mark as prerelease'
        required: false
        default: 'true'

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0  # Fetch all history for git describe

    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: '1.23.6'
        cache: true

    - name: Install dependencies
      run: |
        go mod download
        go mod verify

    - name: Build Linux binary
      env:
        VERSION: ${{ github.event.inputs.version || '1.0.0' }}
      run: |
        GOOS=linux GOARCH=amd64 go build \
          -ldflags "-X github.com/OptioNetwork/optio/app.Version=v$VERSION" \
          -o build/optio-linux \
          ./cmd/optiod
        ls -la build/

    - name: Upload binary artifact
      uses: actions/upload-artifact@v4
      with:
        name: optio-linux-${{ github.event.inputs.version || '1.0.0' }}
        path: build/optio-linux
        retention-days: 30

    - name: Show binary info
      run: |
        echo "Binary size:"
        ls -lh build/optio-linux
        echo "Binary version info:"
        ./build/optio-linux version

  release:
    needs: build
    if: github.event.inputs.create_release == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Download binary artifact
      uses: actions/download-artifact@v4
      with:
        name: optio-linux-${{ github.event.inputs.version || '1.0.0' }}
        path: build/

    - name: Create Release
      uses: softprops/action-gh-release@v2
      with:
        tag_name: v${{ github.event.inputs.version || '1.0.0' }}
        name: v${{ github.event.inputs.version || '1.0.0' }}
        body: |
          ## v${{ github.event.inputs.version || '1.0.0' }}
          
          ### Downloads
          - **Linux (x86_64)**: `optio-linux`
          
          ### Installation
          ```bash
          # Download and make executable
          chmod +x optio-linux
          
          # Move to your PATH
          sudo mv optio-linux /usr/local/bin/optiod
          ```
          
          ### Version Info
          Built with Go 1.23.6
        draft: false
        prerelease: ${{ github.event.inputs.prerelease == 'true' }}
        files: build/optio-linux
        token: ${{ secrets.GITHUB_TOKEN }} 